[Unit]
Description=Moves the contents of /var/log to RAM during boot and keeps it there until shutdown/reboot, when it copies the contents back to permanent storage.
Before=syslog.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/sh -c '/home/tux/unsyncvar start'
ExecReload=/bin/sh -c '/home/tux/unsyncvar reload'
ExecStop=/bin/sh -c '/home/tux/unsyncvar stop'

[Install]
WantedBy=multi-user.target