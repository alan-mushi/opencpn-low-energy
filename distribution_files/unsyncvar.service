[Unit]
Description=Moves the contents of /var/log to RAM during boot and keeps it there until shutdown/reboot, when it copies the contents back to permanent storage.
Before=syslog.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/bin/sh -c '/etc/init.d/unsyncvar start'
ExecReload=/bin/sh -c '/etc/init.d/unsyncvar reload'
ExecStop=/bin/sh -c '/etc/init.d/unsyncvar stop'

[Install]
WantedBy=multi-user.target
